# Prototype → Production Boundary

**Purpose:** DreamTeam AI is a **design-validation prototype with a production track underway**. This document is the single source of truth for what is demo-only, what carries forward to the enterprise product, and what is now live. Read this before building or selling anything.

_Last updated: 2026-07-05_

---

## 0. Production track status — Customer section (P1 + P2 SHIPPED)

The Customer entity section is now a **real product** for real tenants, backed by Supabase:

**P1 — data layer (done):**
- Schema: `supabase/migrations/011_customer_entity.sql` — `customer_accounts`, `support_tickets`, `renewal_invoices`, `human_tasks`, `activity_events`. All tenant-scoped with RLS (profiles → tenant_id lookup), money in cents, `updated_at` triggers, tenant indexes. **Must be applied via Supabase SQL Editor** — the frontend degrades gracefully (provisioning notice) if it hasn't been.
- API layer: `src/lib/customerApi.ts` — typed CRUD, invoice generation with a $10K human approval gate, `decideHumanTask` (persists decided_by, flips gated invoices to `sent`, appends activity), CSV import with a robust client-side parser (`parseCsv`).
- Mode switching: `src/lib/dataMode.ts` + AuthContext — tenants other than the demo tenant (`a0000000-…0001`) or the dev demo login are **live**; the Sidebar company picker shows the real tenant as the primary workspace with TCP/PWC under a "Demo companies" divider so live users can still explore the demo story.
- Live pages: Command Centre (real KPIs + activity feed), Customer Success (real accounts + Add/Import), Support (real tickets table + stats), Renewal & Expansion (real invoices, gated generation), Human Tasks (real approve/reject persisted to DB). Import modal: `src/components/ImportCustomersModal.tsx` (Accounts/Tickets tabs, paste or file, auto column mapping, per-row error report).

**P2 — real DE pipeline (done):**
- Schema: `supabase/migrations/012_knowledge_docs.sql` — `knowledge_docs`, `de_conversations`, `de_messages`, RLS same pattern as 011. Applied and verified.
- Brain: `supabase/functions/de-answer/index.ts` (deployed) — resolves tenant from the caller's JWT, retrieves the tenant's `knowledge_docs` by keyword overlap (top 3, ~6K char cap), calls Claude (`claude-sonnet-5`) with a grounded-only system prompt returning strict JSON `{answer, confidence, sources, needs_escalation}`, persists the conversation, and **auto-escalates** (real `human_tasks` row + `activity_events`) when confidence < 60 or the model asks for escalation.
- Knowledge upload: Knowledge → Library in live mode is the tenant's real `knowledge_docs` (add/paste, .txt/.md upload, edit, delete) — `src/lib/knowledgeApi.ts`, `src/pages/tenant/knowledge/LiveKnowledgeLibrary.tsx`. Demo mode keeps the seeded 4D library.
- Chat: `DEChatDock` in live mode fronts the real DE (Alex, Customer Support DE) — real confidence chip, "From: <doc titles>" sources line, true escalation banner linking to Human Tasks. Demo mode keeps the scripted intents.
- **Activation:** the pipeline is deployed but dormant until the `ANTHROPIC_API_KEY` Edge Function secret is set — see `docs/ACTIVATE-DE.md`. Until then the dock shows an honest "DE brain not yet activated" notice.

**P2.5 — scale foundation (done):** account dimension (4-level tenancy), pgvector chunk retrieval with free edge embeddings, semantic answer cache, usage metrics, end-user widget surface (widget keys + public widget-ask + embeddable widget.js). See SCALING-ARCHITECTURE.md.

**P3 — the Workforce Engine (SHIPPED):** What separates a Digital Employee from a chatbot is the employee machinery around it. All three commitments are live:
1. **Guardrail enforcement in the real path** — `guardrail_rules` (migration 015), managed in Compliance & Guardrails live mode ("Install starter guardrails" for empty tenants). Enforced NOW in the non-LLM path: `generateInvoice`/`decideHumanTask` read the tenant's `require_approval_over_cents` rule (fallback $10K) and record a `guardrail_check` audit event either way. Enforced in the LLM path: `de-answer` and `widget-ask` check every generated answer against active blocking `blocked_phrase`/`blocked_topic` rules — matches are withheld, replaced with a safe message, escalated to Human Tasks, and recorded as `guardrail_block`. *Honest notes:* the answer check is simple case-insensitive pattern matching (v1 — no LLM judge), and it executes only when `ANTHROPIC_API_KEY` is set (deployed dormant until then, same as the P2 brain).
2. **One real playbook end-to-end** — `renewal_v1` (`playbook_runs` + `src/lib/playbookApi.ts`): check account → generate invoice → guardrail check → human gate → send → record. Gated runs pause (`waiting_approval` + `waiting_task_id`); deciding the Human Task resumes (approve → sent + cadence Day-0 + complete) or cancels (reject) the run. Run/steps UI on Renewal & Expansion; run history on Playbooks live mode. *Honest note:* orchestration was client-side v1 — **superseded by R4 below (server-side executor shipped)**.
3. **Real immutable audit events** — `audit_events`: INSERT-only (no update/delete RLS policies **plus** a trigger raising on UPDATE/DELETE, verified against a direct superuser attempt), hash-chained (`hash = sha256(prev_hash‖tenant‖action‖detail‖created_at)`) inside the SECURITY DEFINER RPC `append_audit_event` with a per-tenant advisory lock. `verify_audit_chain` recomputes the whole chain server-side — surfaced as the "Verify chain" button on the Audit Trail live page. Writers: invoice generation, approvals, guardrail config changes, DE resolutions/escalations/blocks, every playbook step.
Deferred behind these: more channels, more integrations, retrieval polish, LLM-judge guardrails.

**R4 — server-side playbook executor (SHIPPED):** orchestration is now **server-authoritative**. The `playbook-execute` edge function (deployed) runs every renewal_v1 step with the service role — guardrail threshold + trust-dial composition, invoice, human gate, hash-chained audit events — so runs survive closed tabs. Idempotent: advancing a run whose gate task is undecided is a no-op; completed/cancelled runs are never re-executed. Human-gate resume is server-side too: `resume_playbook_on_task` (migration 016, SECURITY DEFINER) does the whole resume in SQL (status flips, invoice → sent + cadence Day-0, audit events); `decideHumanTask` calls the RPC, with an edge-function `advance` fallback if the migration is missing. `src/lib/playbookApi.ts` is now a thin start/observe/cancel client; the run panel shows a "server-run" chip. Verified end-to-end via curl: start → waiting_approval → task approved → run completed, audit chain grown and intact.

**R5 — trust dial v1 (SHIPPED):** `de_autonomy` (migration 016) stores per-tenant, per-action autonomy: `invoice_auto_send` (max amount), `answer_dock` / `answer_widget` (confidence floors, dormant until R1 activation). **Composition rule: autonomy narrows within guardrails, never overrides them** — an invoice auto-sends only when it passes BOTH the guardrail approval threshold AND (no autonomy row OR enabled with amount ≤ its max). Enforced now in `generateInvoice` and the playbook-execute gate; auto-sends under the dial are labelled "auto-approved under trust dial (≤ $X)" in the step timeline and the audit event records the autonomy rule id. First live DE-profile surface: Workforce → Digital Employees live mode shows Alex + the trust dial panel (`src/pages/tenant/LiveWorkforceDEs.tsx`) with an evidence line computed from approval audit events; every dial change appends a `config_change` audit event.

**R6 — tenant playbook builder (SHIPPED):** tenants build their own playbooks from a **typed step-primitive registry** (documented in `supabase/functions/playbook-execute/index.ts`): `check_account`, `generate_invoice` (runs the full R5 guardrail + trust-dial composition), `human_approval` (explicit gate — skipped when the invoice auto-approved within limits, mirroring renewal_v1), `guardrail_check` (audited re-check point), `connector_action` (Zendesk write-back with **honest degradation** — no connector/target → recorded `skipped`, run continues), `update_record` (whitelisted status flips only), `log_activity`, `complete`. Templates `{{account.name}}` / `{{invoice.amount}}` / `{{run.id}}` render from run context. Schema: migration 019 — `playbook_definitions` (draft→published→archived, tenant RLS) + `playbook_versions` (**immutable publish snapshots**; runs bind to a snapshot version, never the live draft); `playbook_runs` gained `definition_id`/`definition_version`/`context`. Publishing goes through the edge function's `publish` action: server-side validation with structured errors (unknown primitive, bad params, >20 steps, step-ordering rules, human_approval-after-generate_invoice, post-gate primitive restrictions) → version bump + snapshot + `config_change` audit. **Resume split (honest):** `resume_playbook_on_task` (replaced in 019) advances post-gate steps natively in SQL (guardrail_check/update_record/log_activity/complete — zero HTTP); a post-gate `connector_action` parks the run in `resume_pending` and the edge `advance` (fired by decideHumanTask) finishes it. Builder UI on Playbooks live mode (`src/pages/tenant/systems/LivePlaybookBuilder.tsx`): step editor with per-primitive param forms, live validation (client mirror; server is the authority), reorder, template help, publish, run-with-account picker, per-run step timelines. Legacy `renewal_v1` path untouched — regression-verified via curl alongside the full definition E2E: publish → gated run → SQL resume (parked `resume_pending`) → HTTP advance → completed with connector step honestly `skipped`, audit chain intact. *Honest notes:* manual trigger only (`schedule`/`event` reserved); one invoice + one gate per playbook in v1; `update_record` whitelist is invoice/ticket status only.

**R7 — scheduled & event playbook triggers (SHIPPED):** the `trigger_type` slots reserved in R6 are now wired. Schema: migration 020 — `playbook_schedules` (**friendly cadences**: daily/weekly/monthly at a UTC hour — deliberately NOT raw cron strings; `next_fire_at` computed by a server-side trigger on every write; `account_selector` = single account or all accounts with a renewal date within N days), `playbook_event_rules` (v1 event keys: `invoice_overdue` w/ `overdue_days` param default 7, `ticket_synced_high_priority` w/ `priority` param default p1; **per-target cooldown dedup** via `cooldown_hours`, default 24), `playbook_trigger_fires` (the firing log/queue: `pending_start` → `started`/`error`, or `skipped_dedup` recorded once per cooldown window — the dedup decision itself is auditable without log spam). Dispatcher split: `dispatch_due_triggers()` (SECURITY DEFINER, pure SQL, service-role-only grant) evaluates due schedules + event matches into `pending_start` fires and stamps `last/next_fire_at` atomically (no double-fire even if the HTTP processor crashes); the `playbook-execute` edge function's `dispatch` action (v3) turns pending fires into real definition runs via the same start path as manual runs — guardrail + trust-dial composition, human gates, and hash-chained audit all apply; each fire appends a `playbook_step` audit event with `detail.kind='trigger_fire'`. **Dispatch wiring achieved (verified live on the project):** pg_cron 1.6.4 + pg_net 0.20.3 + Vault available → primary path is the `playbook-dispatch-5min` pg_cron job (every 5 min) calling `invoke_playbook_dispatch()`, which pg_net-POSTs to the edge function authenticated by the Vault secret `playbook_dispatch_secret` (provisioned out of band — never committed; mirrored as the `PLAYBOOK_DISPATCH_SECRET` function secret). Live ticks confirmed in `cron.job_run_details`. Backup path: the Playbooks page fires an opportunistic `dispatch` on load, scoped server-side to the caller's tenant. UI (`LivePlaybookBuilder.tsx`): per-definition Triggers section (cadence/hour/target pickers, event params + cooldown, pause/resume, next-fire preview, fires history with run links) + trigger badges on the library table. E2E verified: due schedule → fire `started` → run completed; overdue sent invoice → event fire → run started; second dispatch inside the cooldown → `skipped_dedup`; audit chain re-verified intact after all fires; seeds cleaned (audit rows remain). *Honest notes:* friendly cadences only (no cron expressions in v1); two event keys; events are polled on the 5-minute tick, not pushed (webhooks are the upgrade); dispatch processes up to 25 fires per tick.

**Customer Success — end-to-end (SHIPPED):** the Success stage of the Customer Lifecycle is live product, not demo. Schema: migration 021 — `health_score_config` (one row per tenant: signal weights default tickets 25 / escalations 25 / overdue invoices 30 / activity recency 20, thresholds default at-risk < 50 / healthy > 75, `last_computed_at` freshness stamp) + `customer_accounts.health_components` (the transparent breakdown). **Health is COMPUTED from real signals, never hand-entered:** `compute_account_health(_core)` (SECURITY DEFINER, membership-guarded; service variant for the cron path) scores 0–100 by subtracting bucketed penalties — open/pending ticket count, escalated tickets (heavier), overdue *sent* invoices (heaviest), and days since the account's last `activity_event` (recency decay) — and writes both the score and the per-component breakdown onto the account row. Status flips active↔at_risk per the tenant thresholds (**churned is never touched**), with exactly ONE hash-chained audit event per flip (`config_change`, `detail.kind='health_recompute'`) — verified: two consecutive recomputes produced one audit row. Recompute paths: (a) **nightly via the existing pg_cron dispatcher** — `invoke_playbook_dispatch()` gained a pre-step that recomputes tenants whose last compute is >24h old, BEFORE the dispatch POST so a fresh at-risk flip fires the very same 5-minute tick (verified live: `health:1` then fire → run); (b) opportunistic on Success page load (server no-ops under 1h freshness); (c) "Recompute now" button. **The payoff loop:** new `account_at_risk` event key on the R7 trigger machinery (optional `min_arr_cents` filter; per-account cooldown dedup with the exact 020 `skipped_dedup` semantics) — an account sitting at-risk automatically starts the bound playbook. E2E verified on the live project: seeded 1 open + 1 escalated ticket + 1 overdue sent invoice + no activity → score 34 (10+15+21+20 penalties), status flipped, dispatch → fire `pending_start` → cron path → run `started` → `completed`; second dispatch inside cooldown → `skipped_dedup`; seeds cleaned, audit rows kept. UI: Success page live mode rebuilt (`CustomerSuccessLive.tsx`) — header stats (accounts / at-risk / avg health / **ARR at risk**), health bars with component-breakdown popovers ("Tickets −10 · Escalations −15 · Overdue invoice −21 · Activity −20" — the transparency IS the feature), account drawer (open tickets, overdue invoices, recent activity, trigger-fire history, run-playbook picker), collapsible scoring config (weight sliders + thresholds, saves audited). *Honest notes:* no health history in v1 — only the latest computed breakdown; activity recency reads from `activity_events` only (connector activity lands there via existing paths); recompute is polled (5-min tick / page load), not pushed.

**Customer Onboarding — end-to-end (SHIPPED):** the founder's original use case is live product: an implementation workspace where onboarding projects run against templated checklists with task assignment, progress, and human sign-offs. Schema: migration 022 — `onboarding_templates` (tenant-editable drafts: ordered items with key/label/phase kickoff→data→config→validation→golive, owner_type human/de/either, requires_signoff), `onboarding_template_versions` (**immutable publish snapshots**, same doctrine as playbook_versions — projects bind to a snapshot, never the live draft; publish validates server-side: 1–50 items, ≥1 go-live item, unique keys, and **sign-off items must be owned by human/either — a DE cannot sign off its own work**), `onboarding_projects` (account + template version + per-item `items_state` status/assignee/note; `progress_pct` recomputed by trigger on every write). **Sign-off gates ride the existing Human Tasks machinery:** marking a requires_signoff item done creates a `review_gate` human task; `decideHumanTask` calls `resolve_onboarding_signoff` (SECURITY DEFINER) **alongside — never replacing — the playbook resume hook** (guarded by `related_table='onboarding_projects'`, so playbook gates are untouched); approve → item `signed_off` (locked), reject → back to `in_progress` with a rework note. Completion is server-side: every non-sign-off item done AND every sign-off item signed off → project auto-completes with audit + activity ("Onboarding completed — {account}"). Audit (hash-chained, `detail.kind='onboarding_*'`): template publish, project create/hold/cancel/complete, every sign-off decision. Activity: exactly one event per item status transition (note/assignee edits are silent). Starter: `install_starter_onboarding_template()` — 10 SaaS items across the 5 phases with 4 sign-off gates (settings review, leave rules, UAT, go-live), published on install. UI: live branch of the Onboarding page (`CustomerOnboardingLive.tsx`; the 1,200-line co-pilot demo page is untouched for demo companies) — project cards (progress, phase chips, go-live countdown), phase-grouped checklist detail (status select, assignee, note popover, amber awaiting-sign-off state linking to Human Tasks), template editor (add/reorder/remove, phase/owner/sign-off flags, publish with structured validation errors, version chips); Success account drawer shows the account's onboarding project. E2E verified on the live project via SQL-as-user: install → publish v1 → project on seeded account → item done (activity event) → sign-off item done (task created, progress 20%) → reject (item back to in_progress + rework note) → all items done + 4 approvals → auto-completed at 100%, audit chain re-verified intact, seeds cleaned (audit rows remain). *Honest notes:* assignee is free text in v1 (the people directory is still demo data); data items are checklist confirmations — no file upload in v1; **DE-assisted onboarding (AI column mapping, config drafting — the co-pilot the demo page shows) is the R1-activation upgrade**, not built yet; sign-off decisions reflect on page reload, not push.

**BD + Sales — end-to-end (SHIPPED):** the FINAL Customer Lifecycle stage — the pipeline is live product, and it closes the loop. **SoR framing (doctrine, SCALING-ARCHITECTURE.md §Systems-of-Record): the CRM stays the system of record for pipeline.** `opportunities` (migration 023) is a **working cache / action workspace**, not a CRM replacement: every row carries `source` ('native'|'import'|future CRM providers) + `external_ref` exactly like `support_tickets`; CSV import is explicitly labeled *bootstrap*; the Salesforce/HubSpot connector is the designed-in sync upgrade (`connector_objects` already supports arbitrary object types — 'opportunity' is the sync target). Schema: stage prospect→qualified→proposal→negotiation→won/lost with **guarded transitions** — `stage_history` jsonb appended by trigger on every change (+ one activity event; audit `detail.kind='opportunity_closed'` on won/lost only), won/lost reachable ONLY via the close RPCs (DB trigger rejects direct writes via a transaction-local flag), lost REQUIRES a reason, closed opportunities are stage-immutable. **The lifecycle spine (the point of the whole build): `close_opportunity_won` — BD → Sales → won → customer account exists (created from `company_name`, ARR = deal amount v1, or linked to an existing account) → onboarding project starts (022 RPC, optional template picker) → success monitors health (021) → renewal plays run (020). Winning a deal is the moment a prospect becomes a customer; the full Customer Lifecycle loop now closes with zero re-entry.** Trigger: new `opportunity_won` event key on the R7 machinery (optional `params.min_amount_cents`, 7-day recency window, per-opportunity cooldown dedup with exact 020/021 semantics) — winning a deal auto-fires a welcome/kickoff playbook. `pipeline_summary` view (security_invoker): per-stage count+amount, win rate last 90 days. UI (`PipelineLive.tsx`, live branches of the BD + Sales pages; demo branches untouched): **one opportunities table, two lenses** — BD = top-of-funnel (prospect list, add/import, Qualify button hands the row to Sales), Sales = deal lens (qualified→won/lost, table + inline stage select, no drag-drop v1); both show the summary strip and the "Your CRM stays your CRM" banner. Won flow modal: create-or-link account (auto-suggests a name match) → optional start-onboarding with template picker (installs the starter inline if none) → toast pointing at the new account + project; Lost flow requires a reason; Success account drawer shows "Won from pipeline" origin. E2E verified on the live project via SQL-as-user: create → qualify → proposal (stage_history 3, activity events), direct-won write REJECTED by trigger, lost-without-reason REJECTED, close won w/ onboarding → account created (ARR = amount) + 10-item project live + `opportunity_closed`/`opportunity_won_handoff` audit rows; separate lost path (reason recorded, closed row stage-immutable); `opportunity_won` rule → dispatch → fire `started` → run `completed` (welcome activity rendered from template), second dispatch inside cooldown → `skipped_dedup`; triple-hook regression: playbook resume no-ops gracefully on non-playbook tasks, onboarding sign-off approve → `signed_off`, wrong-task guard intact; audit chain re-verified intact, seeds cleaned (audit rows remain). *Honest notes:* owner is free text v1 (like onboarding assignee); ARR = deal amount verbatim on won (no annualization math); no drag-drop kanban v1; the CRM connector itself is NOT built — it's the sync upgrade triggered by the first tenant with a CRM.

**Specialists v1 — Technical (SHIPPED):** specialists are the GP/consultant model made real product — consulted when completing tasks, highly configurable, connected to the customer's data on the customer's terms. Schema: migration 024 — `specialist_profiles` (key technical/legal/finance/people, tenant-editable **charter** = the role definition, active/paused; `install_technical_specialist()` seeds the Technical charter), `specialist_sources` with the **access-mode matrix enforced by CHECK constraint** (the customer's storage choice, per source: knowledge→ingest only; connector→ingest|fetch_only; mcp_server→fetch_only|reference; link→reference; media→ingest — companies that won't allow storing pick fetch-only/reference and nothing is persisted), `specialist_source_secrets` (MCP auth, service-role-only isolation like connector_secrets), `media_assets` (documents/images/videos in the private `specialist-media` bucket w/ tenant-folder storage RLS; tags, sort order, **quality_flags** stale/incomplete/conflicting/unreadable with note+raiser, all audited; .txt/.md extracted client-side into a linked tag-scoped knowledge_doc so they're consultable NOW, pdf/docx/video/image stored with honest `extracted=false` — "indexed by title/tags; content extraction on activation"), `spec_consultations` (every consult recorded: requester human/de/playbook, run linkage, sources_used citations, status answered/blocked_llm/escalated/error). Brain: `specialist-consult` edge function (deployed) — retrieval per access mode (tag-scoped `match_doc_chunks` w/ keyword fallback; connector fetch_only = live read-through ONLY when the consult context names a target ref, audited, nothing persisted; connector ingest = the synced working cache; MCP/link = cited references), then the same grounded-only JSON contract + blocking-guardrail answer check + confidence-floor escalation as de-answer. **Dormant-honest:** no ANTHROPIC_API_KEY → `blocked_llm` consultation row + `retrieved_sources` returned so the plumbing is verifiable now (verified live: tag-scoped doc retrieved, fetch-only honestly skipped without a ref, MCP/link cited). Playbook primitive `consult_specialist` (executor + client mirror + builder UI): profile/question template/min-confidence/on_low escalate-or-continue; dormant/missing profile → step `skipped` honestly, escalation task still fires when chosen (E2E verified: run completed, step skipped, `blocked_llm` row with `requested_by='playbook'` + run_id, escalation task created). **The Scribe sub-specialist — structural anti-hallucination, not prompt-based:** `scribe_requests.consultation_id` is **FK NOT NULL** (no consultation, no write — DB-enforced, negative-tested), payloads are built **server-side from a whitelisted template per action** interpolated from the stored consultation's question/answer/citations (update_status values are a closed enum; the model/caller supplies no free text), **every request creates a human task** (always-gated v1; trust-dial expansion is a later upgrade), and every outcome is audited with the consultation citation chain (`connector_action`, `detail.kind='scribe_write'`). decideHumanTask hook #3 (additive, guarded by `related_table='scribe_requests'`; hooks 1–2 regression-verified) executes on approve — verified live: approve → real Zendesk write attempted → fake instance → honest `failed` with the structured 404 recorded; reject → `rejected`. UI: Specialists → Technical live branch (`SpecialistLive.tsx`; demo pages untouched) — charter editor + pause toggle (audited), sources manager with matrix-constrained access-mode picker + MCP register/test (ping verified → reachable chip), media library (upload/tags/sort/quality flags/extraction chips), consultation console (retrieved_sources + dormant notice now; answers on activation), Scribe queue; legal/finance/people show the honest "configure coming — install pattern proven on Technical" card. *Honest notes:* MCP = registration + connectivity ping only (full MCP client session is the upgrade); pdf/docx/video/image content extraction on activation; the Scribe is always-gated v1; the consult LLM path is dormant until the key.

**R2 — Systems-of-Record connector layer v1 (SHIPPED):** the SoR doctrine made real (see SCALING-ARCHITECTURE.md §Systems-of-Record).
- Schema: `supabase/migrations/017_connectors.sql` — `connectors` (tenant-scoped, RLS), `connector_secrets` (**service-role-only**: RLS enabled with zero authenticated policies; written only via the `set_connector_secret`/`purge_connector_secret` SECURITY DEFINER RPCs, never readable from the client), `connector_objects` (per-object mode `sync` vs `read_through` + interval + enable), `connector_actions` (write-back registry). `support_tickets` gained `external_ref` + `source` (unique on tenant+source+external_ref) so synced tickets are a keyed working cache. `audit_events` categories extended with `connector_sync`/`connector_action`.
- Zendesk connector v1: `supabase/functions/connector-zendesk` (deployed) — `test` (auth check), `sync_tickets` (incremental pull → upsert into `support_tickets`, **300-ticket cap per run**, status/priority mapping documented in-source, account match via requester-org `external_ref`), `read_ticket` (read-through: fetched live, returned, **nothing persisted except the audit event**), `write_back` (`add_internal_note`/`update_status` PUT back into Zendesk, gated by the action registry, audited).
- UI: Connectors page live mode (`LiveConnectorsPage.tsx`) — connect flow (Test & Save), per-object mode table, write-back toggles, Sync now, read-through demo, disconnect (credential purge). Support page shows a "Zendesk" source chip on synced tickets.
- *Honest notes:* v1 is Zendesk only (Zuora/Salesforce/Workday are catalog placeholders); tokens sit in a service-role-only table — Vault/KMS encryption is the hardening step; write-back ops are note+status only; per-DE credential scoping and webhook/TTL auto-refresh are next; verified to the credential boundary (real Zendesk trial needed for end-to-end proof).

**R3 — live Proving Ground v1 (SHIPPED):** golden Q&A eval suites run against the REAL DE. Schema: `supabase/migrations/018_proving_ground.sql` — `golden_qa` (question, expected_fragments text[], min_confidence, category, active; tenant RLS), `eval_runs` (tenant-read RLS; writes only via the runner's service role), `eval_gate` view (latest finished run per tenant, security_invoker). Runner: `supabase/functions/eval-run` (deployed, JWT-authed) — loads the tenant's active questions (hard cap 50, sequential + 250ms delay: cost-bounded), calls the deployed `de-answer` over HTTP **forwarding the caller's JWT** (tenant scoping stays honest — verified end-to-end), grades pass = all expected fragments present (case-insensitive) AND confidence ≥ floor, writes progress after each question, appends an audit event (`config_change` + `detail.kind='eval_run'`). If de-answer returns `llm_not_configured` the run finishes as **`blocked_llm`** — verified live against the demo tenant (runner reached the real LLM gate and stopped honestly). Publish gate: `getEvalGate()` in `src/lib/evalApi.ts`; LiveKnowledgeLibrary checks it before create/upload — a failing latest run opens an override dialog ("Publish anyway" is audited as `eval_gate_override`, with a link to the Proving Ground). UI: Proving Ground live mode (`LiveProvingGround.tsx`) — suite editor, starter-suite generator (5 template questions from knowledge doc titles — honest client-side v1, editable), live-polled run results, run history, gate banner. *Honest notes:* grader v1 is fragment matching + confidence calibration (LLM-judge grading is the upgrade); the publish gate is client-side soft (server-side hard gate in ingest-chunks is the hardening step); runs are dormant (`blocked_llm`) until the ANTHROPIC_API_KEY lands.

**Earned-Trust Progression (SHIPPED):** autonomy is EARNED per action category from measured evidence, promoted by a human, demoted automatically on regression — "promote slow, demote fast." **Guardrails always cap — promotion only widens the trust dial (`de_autonomy`) within guardrails; the 016 composition rule is untouched.** Schema: migration 025 — `trust_policies` (one row per tenant × action category: earned level 0–3, promotion criteria jsonb — eval pass rate/samples, human approval rate/samples, max guardrail blocks, window days; SELECT-only RLS — every write goes through SECURITY DEFINER RPCs). The ladder (`trust_level_settings`): invoice_auto_send L1 ≤$1K / L2 ≤$5K / L3 ≤$10K; answer categories L1 conf≥90 / L2 ≥75 / L3 ≥60. Evidence (`compute_trust_evidence`, membership-guarded, server-computed ONLY — the client never asserts it) reads three sources: Proving Ground `eval_runs` (pass rate + samples in window), `human_tasks` outcomes (approval rate), `audit_events` guardrail blocks. Promotion: `request_trust_promotion` recomputes evidence and REJECTS server-side if any criterion is unmet; eligible → `trust_promotion` human task with evidence snapshot; decideHumanTask **hook #4** (additive — hooks 1–3 untouched and regression-checked) calls `apply_trust_promotion`, which **blocks self-approval** (requester ≠ approver, enforced in SQL) and **re-verifies evidence is STILL eligible at apply time** (regressed since request → rejected as stale) before moving the dial one level. Demotion is automatic, server-side, immediate — no human gate on the way down: triggers on `eval_runs` completion (pass rate below the policy floor) and on `guardrail_block` audit events (zero-tolerance default) drop one level (never below baseline), audit `trust_demoted`, and create an informational `trust_demotion_notice` task. All trust events ride the hash-chained audit trail as `config_change` + `detail.kind='trust_*'` (same pattern as the 018 eval events). Manual dial raises above the earned level still work but are labeled **"Manual override"** in the UI and audited as `trust_manual_override` — the earned path is the celebrated one. UI: "Earned trust" panel on the live Digital Employees page (`LiveWorkforceDEs.tsx`) — per-category evidence bars in plain language ("29 of 30 evaluated answers passed in the last 30 days"), Eligible badge, Request-promotion button, promotion/demotion history from the audit trail. Verified live with negative tests: insufficient evidence → request rejected server-side; seeded passing evidence → task with snapshot → self-approval blocked → second user approved → `de_autonomy` actually moved (enabled, ≤$1,000) → `verify_audit_chain` intact (43 checked, 0 broken); failing eval run inserted after a request → apply rejected as stale; failing eval run → automatic demotion (level 1→0, dial disabled, `trust_demoted` + notice task); guardrail block appended by a tenant member → same immediate demotion; demo-tenant seeding refused by the RPC (demo mode untouched). *Honest limits:* evidence sources are evals + human-task outcomes + guardrail events until LLM activation increases live volume (answer categories default `min_human_samples: 0` for that reason); guardrail-block demotion is tenant-wide, not per-category attributed yet; the self-approval block holds but its audit breadcrumb rolls back with the rejection; demotion triggers ride the two production write paths (eval-run edge fn = service role; guardrail blocks = member/service role) — raw superuser SQL bypasses the audit guard and is not a supported path.

**Connector Hub + Evidence Pipeline (SHIPPED):** answers the "TCP demo" challenge — a support DE that can check the customer's product for account configuration, read knowledge for the accurate answer, and verify against past support conversations before responding. Schema: migration 026 — `connectors` widened to zendesk/salesforce/confluence/jira/intercom/generic_rest/sharepoint, plus per-connector `role` (product_system/crm/support_desk/knowledge_base/other — the evidence pipeline routes by this) and `access_mode` (ingest/fetch_only — the customer's storage choice); `evidence_runs` (tenant-read RLS, service-role writes). Edge functions: `connector-hub` (one adapter interface per provider: test/search/fetch_record/list_recent/sync; credentials remain in `connector_secrets`, zero client access; knowledge-capable providers ingest articles/pages through the same chunk+embed path as ingest-chunks with `source='connector'` + `external_ref`), `mcp-client` (REAL MCP client over Streamable HTTP — initialize with protocol 2025-03-26, Mcp-Session-Id capture, notifications/initialized, tools/list, tools/call; parses both JSON and SSE responses; handshake stores a tool-list summary on the source row; call_tool is fetch-only + audited), and `specialist-consult` `resolve_inquiry` (the evidence pipeline: account context → internal knowledge + knowledge-base connectors → past-case verification on support-desk/CRM connectors → cited evidence bundle + confidence inputs; every step audited as `evidence_step`; whole run persisted). Doctrine enforced server-side: **fetch-only connectors REFUSE sync** (HTTP 403, audited), and the **pass-through compromise** is that read-through steps persist only citation metadata — title, ref, url, snippet capped at 200 chars — never full payloads. UI: Connectors provider wizard (plain-language role + data-handling choice, per-provider "how to get credentials" help) and "Resolve an inquiry" on the Technical Specialist page (live evidence trail with honest "Not connected — skipped" badges, confidence chips, answer slot marked "Awaiting LLM activation", past runs).
*Honest notes — what is proven vs what needs real tenant credentials:* PROVEN live: generic_rest end-to-end against a REAL external HTTP API (jsonplaceholder at $0 — the account-context and history steps returned real fetched data through the whole pipeline); real MCP handshake + tools/call against a live public MCP server (DeepWiki — server info + 3 tools listed, tool result returned) and honest structured failure against a nonexistent URL; sync-refusal on fetch-only (403 + audit), no-credential provider search → structured `no_credentials`, zero-connector evidence run (all external steps `skipped_not_connected`, run persisted, knowledge step still answered from uploaded docs), snippet cap verified at exactly 200 chars, audit trail written for every call. **UNPROVEN until the founder creates free accounts:** the salesforce / confluence / jira / intercom adapters are code-complete and shaped to each provider's documented REST API (Salesforce OAuth2 client-credentials + SOQL against a Developer Edition; Atlassian basic-auth CQL/JQL; Intercom bearer) but have NOT been exercised against live instances — say this plainly in any demo. SharePoint is registration-only and returns a structured `not_implemented`. The final written answer in an evidence run stays dormant-honest behind `ANTHROPIC_API_KEY`.

**Still design-preview even in live mode:** Business Development, Sales stages of the Customer journey; Vendors & Partners; Workforce entity; Outcomes pages; Knowledge gaps/Intelligence sections; the Customer overview journey-stage stats. (Compliance & Guardrails, Audit Trail, and Playbooks now have real live modes.)

## 1. What this codebase IS today

A polished frontend prototype used to:
- Validate the entity/outcome operating model with real prospects (TCP Software, PWC demo companies)
- Close product design decisions cheaply before they get expensive
- Serve as the visual and interaction spec for the production build

The demo companies remain **not production**. Real customer data is supported only in the Customer-section live pages listed above, for real tenants, behind RLS.

## 2. Demo-only (will be REPLACED, do not invest in hardening)

| Piece | Why it's disposable |
|---|---|
| `src/data/companies.ts`, all page seed data | Hardcoded demo numbers; replaced by real APIs |
| localStorage persistence (health configs, DE profiles, thresholds) | Browser-only, per-device; replaced by tenant DB |
| Company switcher (TCP/PWC) | Demo device; production = one tenant per org, real tenancy |
| Rule-based "agent brain" (keyword scorer + FT search) | Replaced by real LLM orchestration layer |
| Remaining legacy pages awaiting Phase-4 migration (CustomerPortal, FinanceControlTower, ImplementationWorkspace, DataConnectors, Playbooks, Approvals, KnowledgeHub/Data, AuditLog, Security) | Content migrates into new pages, then files are deleted |
| Mock auth users / demo tenants | Replaced by full Supabase (or successor) auth + RBAC |

## 3. Production-intent (KEEPS its value — protect and refine)

| Asset | Where it lives |
|---|---|
| **Domain model** — DE, Guardrails (industry template + customer overrides + per-DE restrictions), Human-Loop touchpoints, Knowledge taxonomy (Entity × Audience × Type × Confidence), gap-detection → resolution loop, configurable self-learning with validation gates | Memory docs + implemented shapes in `WorkforceDEsPage`, `DashboardPage` types |
| **Information architecture** — entity/outcome navigation, legacy-department mapping for transition, Command Centre layout | `Sidebar.tsx`, `App.tsx` routing, `DashboardPage.tsx` |
| **UI components and visual system** — dark theme, card/table/badge patterns, 10-tab DE profile | `src/pages/tenant/*`, `src/components/*` |
| **Architecture doc** — 10 service layers, service boundaries, tenant-isolation and vendor-independence rules | Project memory (`architecture.md`) |
| **TypeScript interfaces** for DE profile, guardrails, human loop, health config | `WorkforceDEsPage.tsx`, `DashboardPage.tsx` — extract to `src/types` during production build |

## 4. Gate criteria — when to start the production track

Start the production build when **all** of these are true:

1. **Product model is stable** — Phases 4–7 complete and the entity/outcome + DE profile design has survived at least 3 real prospect walkthroughs without structural change requests.
2. **A committed design partner exists** — at least one customer with a signed pilot (or strong LOI) willing to run real data through the system.
3. **The core loop is specified end-to-end on paper** — for one concrete use case (e.g. support ticket resolution): data in → DE acts → guardrails checked → human gate → audit written. No unresolved "TBD" in that chain.
4. **Budget/runway supports a 4–6 month backend build** — multi-tenant schema, API layer, LLM orchestration, audit infrastructure, security review.

## 5. Production build — what gets built fresh (not migrated)

- Multi-tenant Postgres schema with row-level security; **no cross-tenant access, ever**
- API layer between UI and data (UI never queries tables directly)
- LLM orchestration: provider-abstracted, confidence scoring, fallback routing
- Immutable audit log (append-only), guardrail versioning
- Real ingestion pipeline (embeddings, chunking, citations)
- Observability, rate limiting, SSO/SCIM, pen-test before first real tenant

The frontend is **rewired to real APIs, not rewritten**. Estimated reuse: most of the UI, ~all of the domain model, none of the data layer.

## 6. Rules until then

- New features go into the prototype fast and cheap — do not gold-plate demo code
- Never present the prototype as production-ready to a customer; it's a design preview
- Any real customer data requires the production track — no exceptions
- Keep this document updated when the boundary moves
