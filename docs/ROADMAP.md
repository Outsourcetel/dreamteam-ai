# DreamTeam AI — Production Roadmap

_Formalized 2026-07-04 from the on-target scorecard (overall ~6.8/10). Companion to [PROTOTYPE-PRODUCTION-BOUNDARY.md](PROTOTYPE-PRODUCTION-BOUNDARY.md) and [SCALING-ARCHITECTURE.md](SCALING-ARCHITECTURE.md). Governing doctrine: DEs act like employees across existing systems of record, inside the judgment machinery (Workforce Engine). Every item below deepens the machinery — nothing decorates._

## R1 — Activation & E2E proof  `blocked on: founder's Anthropic API key`
Set ANTHROPIC_API_KEY edge secret → full end-to-end test: knowledge upload → grounded answer w/ sources → guardrail violation blocked + escalated → immutable audit chain verified → widget path → cache write confirmed. Fix known gap: widget-ask pre-LLM usage_metrics not incrementing. TODO(R1-activation): wire the `de_autonomy` answer-confidence floors (`answer_dock` / `answer_widget`, stored by R5 migration 016 and configurable on the trust dial) into de-answer and widget-ask — dormant until the key.
**Moves scores:** Trust 4→5, Live depth 6.5→7.5.

## R2 — Systems-of-Record connector layer v1  ✅ **SHIPPED**
The doctrine-critical gap (scored 2/10). Connector framework implementing the SoR principle:
- ✅ Schema (migration 017): `connectors` + `connector_objects` (per-object mode `sync` vs `read_through`) + `connector_actions` (write-back registry) + `connector_secrets` — service-role-only credential table written via SECURITY DEFINER RPC, never client-readable (Vault/KMS encryption is the hardening step; per-DE scoping next). `support_tickets` gained `source`+`external_ref` as the working-cache key.
- ✅ **Zendesk connector v1** (`connector-zendesk` edge fn, deployed): test / sync_tickets (incremental pull, 300-ticket cap per run, upsert into support_tickets) / read_ticket read-through (nothing persisted except the audit event) / write_back `add_internal_note`+`update_status`. Audit categories `connector_sync`/`connector_action` added.
- ✅ Live Connectors page: connect flow (Test & Save), object-mode table w/ intervals, write-back toggles, sync now, read-through demo, disconnect w/ credential purge. Zendesk source chip on live Support tickets.
- ⏸ Renewal playbook write-back step slot: deferred to the first billing connector (Zuora) — nothing to bind yet.
**Verified to the credential boundary (fake creds return structured `zendesk_auth_failed`, proving the full path to Zendesk's door); founder connects a free Zendesk trial to prove end-to-end.**
**Moves scores:** SoR layer 2→5.

## R3 — Live Proving Ground v1  `SHIPPED — runs dormant until key`
Golden Q&A pairs per tenant (`golden_qa`), eval runner edge function that asks the live DE each question and grades answer-contains-expected + confidence calibration; `eval_runs` history; **knowledge publishes gated on regression** (tenant-overridable, override audited). Live Proving Ground page: suite editor, starter-suite generator, run history, pass/fail detail. Operationalizes DE-machinery learning #9 (verify claims — "done" is not evidence).
**Shipped notes:** migration 018 + `eval-run` edge function (JWT-forwarding to de-answer verified; `blocked_llm` honest-dormant path verified live) + `src/lib/evalApi.ts` + `LiveProvingGround.tsx` + eval-gate dialog in LiveKnowledgeLibrary. Grader v1 is fragment matching (LLM-judge grading is the upgrade); gate is client-side soft (server-side hard gate in ingest-chunks is the hardening step).
**Moves scores:** Trust 4→6 (with R1 active).

## R4 — Server-side playbook executor  `buildable now`
Move renewal_v1 orchestration from browser (`playbookApi.ts`) into a `playbook-execute` edge function: runs advance server-side, survive closed tabs, steps idempotent, human-gate resume triggered by decideHumanTask via RPC. Client becomes start/observe only. Groundwork for the tenant playbook builder.
**Moves scores:** Engine 7→8, Stack 8.5→9.

## R5 — Trust dial v1 (per-action autonomy thresholds)  `buildable now, answer-path dormant until key`
`de_autonomy` config per tenant per action type: auto-approve invoice ≤ $X at confidence ≥ Y%; answer-confidence floor per channel (dock vs widget). Enforced in generateInvoice/playbook gate now, in de-answer/widget-ask on activation. Live UI on the DE profile (first live DE-profile surface) with the evidence line ("94% of Casey's invoices approved unchanged — raise the limit?") fed from audit_events. Productizes learnings #3/#5.
**Moves scores:** Engine 8→8.5, Features 7→7.5.

## R5.5 — Earned-Trust Progression  ✅ **SHIPPED**
The trust dial stops being a knob you set and becomes a level the DE earns. Research basis: **calibrated trust** (autonomy should track demonstrated capability, not vibes), **per-capability thresholds** (trust one action, not the whole agent), **eval-gated staging** (the Proving Ground is the promotion evidence), **promote slow / demote fast** (human approval on the way up, automatic and immediate on the way down). Migration 025: `trust_policies` (earned level 0–3 per action category, criteria jsonb, SELECT-only RLS), server-computed evidence from eval_runs + human_tasks outcomes + guardrail blocks (`compute_trust_evidence`), promotion via `request_trust_promotion` → `trust_promotion` human task → decideHumanTask hook #4 → `apply_trust_promotion` (self-approval blocked, evidence re-verified at apply time — stale regressions rejected), automatic one-level demotion on failing eval runs or guardrail blocks (audit `trust_demoted` + notice task, never below baseline). Manual raises above the earned level are audited as `trust_manual_override` and labeled in the UI so the earned path stays the celebrated one. Guardrails still cap everything — promotion widens autonomy only within them (016 composition rule untouched). *Honest limits:* evidence volume is thin until R1 activation (evals + human reviews + the guardrail record are the only sources today); guardrail demotion is tenant-wide v1.
**Moves scores:** Engine 8.5→9 (trust is earned machinery, not config), Features 7.5→8.

## R6 — Tenant playbook builder  ✅ **SHIPPED**
Tenants compose playbooks from typed step primitives (`check_account`, `generate_invoice`, `human_approval`, `guardrail_check`, `connector_action`, `update_record`, `log_activity`, `complete`) in a builder UI (Playbooks live mode). Definitions (`playbook_definitions`, migration 019) are validated server-side on publish (structured errors: unknown primitive, bad params, >20 steps, gate-ordering rules) and snapshotted into immutable `playbook_versions` — runs execute the snapshot via the generalized `playbook-execute` executor, never the live draft. Guardrail + trust-dial composition applies to every generated invoice exactly as in renewal_v1.
**Honest notes:** manual trigger only in v1 — `schedule`/`event` trigger types are reserved columns, not wired. Human-gate resume is split: `resume_playbook_on_task` advances post-gate steps it can do natively in SQL (guardrail_check / update_record / log_activity / complete); a post-gate `connector_action` needs HTTP, so the RPC parks the run in `resume_pending` and `decideHumanTask` fires the edge function's `advance` to finish it. `connector_action` degrades honestly — no connected connector / no target ref → step recorded `skipped`, run continues. `update_record` is a whitelisted status flip only. Legacy `renewal_v1` runs untouched (regression-verified).
**Moves scores:** Engine 8.5→9, Features 7.5→8.

## R7 — Scheduled & event playbook triggers  ✅ **SHIPPED**
Fills the `trigger_type` slots reserved in R6. Migration 020: `playbook_schedules` (friendly cadences — daily/weekly/monthly at a UTC hour, **deliberately not raw cron strings**; `next_fire_at` computed server-side on every write), `playbook_event_rules` (two event keys in v1: `invoice_overdue` w/ `overdue_days`, `ticket_synced_high_priority` w/ `priority`; per-target `cooldown_hours` dedup, default 24h), `playbook_trigger_fires` (firing log — every dispatch decision is a row: `pending_start` → `started`/`error`, or `skipped_dedup` recorded once per cooldown window). `dispatch_due_triggers()` (SECURITY DEFINER, pure SQL) evaluates due schedules + event matches into `pending_start` fires and stamps schedule `last/next_fire_at` atomically — a crashed HTTP processor can never double-fire a schedule. The `playbook-execute` `dispatch` action turns pending fires into real definition runs (same start path as manual runs — guardrails, gates, and the audit chain all apply) and audits each fire (`playbook_step`, `detail.kind='trigger_fire'`).
**Dispatch mode achieved (verified on the project):** pg_cron 1.6.4 + pg_net 0.20.3 + Vault all live → **primary: pg_cron job every 5 minutes** (`playbook-dispatch-5min` → `invoke_playbook_dispatch()` → pg_net POST with the Vault-held `playbook_dispatch_secret`, provisioned out of band, never in git). Live cron ticks observed in `cron.job_run_details` (succeeded). **Backup: opportunistic dispatch** when the Playbooks page loads, scoped server-side to the caller's tenant.
**Honest notes:** friendly cadences only, not cron expressions; two event keys in v1; events are *polled* on the 5-minute tick, not pushed; `all_eligible` targeting = accounts with a renewal date within N days (default 60). E2E verified: due schedule → fire `started` → run completed; overdue sent invoice → event fire → run; second dispatch inside cooldown → `skipped_dedup`; audit chain intact after all fires.
**Moves scores:** Engine 9→9.5 — playbooks now run without a human pressing the button, inside the same judgment machinery.

## Lifecycle completion

### Customer Success — end-to-end  ✅ **SHIPPED**
Migration 021. Health is **computed from real signals** (open/pending tickets, escalations, overdue sent invoices, activity recency), never hand-entered: each account carries a transparent `health_components` breakdown ("Tickets −10 · Escalations −15 · Overdue invoice −21 · Activity −20"), weights and at-risk/healthy thresholds are tenant-configurable (`health_score_config`), and status flips active↔at_risk automatically (churned never touched; ONE audit event per flip, not per recompute). The payoff loop: new `account_at_risk` event key on the R7 trigger machinery — an account sitting at-risk fires a playbook automatically (optional min-ARR filter, per-account cooldown dedup). Recompute paths: nightly via the existing pg_cron dispatcher (24h staleness check on every 5-min tick, runs BEFORE dispatch so a flip fires the same cycle), opportunistic on Success page load (>1h stale), and a "Recompute now" button. Success page rebuilt: header stats (accounts / at-risk / avg health / ARR at risk), breakdown popovers, account signal drawer with run-playbook, collapsible scoring config. **Honest:** no health history in v1 (latest breakdown only); recency reads from `activity_events` only.

### Onboarding — end-to-end  ✅ **SHIPPED**
Migration 022. The founder's original use case: implementation projects run against **versioned checklist templates** (immutable publish snapshots, same doctrine as playbooks — 1–50 items across kickoff→data→config→validation→go-live, sign-off items must be human/either owned). Per-item status/assignee/note with server-computed progress; **sign-off gates ride the Human Tasks machinery** (done on a sign-off item → `review_gate` task; approve → `signed_off` locked, reject → back to in_progress with a rework note; the hook runs alongside the playbook resume hook, never replacing it); projects **auto-complete server-side** when everything is done/signed off. 10-item SaaS starter template with 4 sign-off gates, one click to install. Hash-chained audit on publish / create / cancel / every sign-off decision; one activity event per item transition. **Honest:** assignee free-text v1; no file upload (data items are checklist confirmations); DE-assisted mapping/config drafting = R1-activation upgrade.

### BD + Sales — end-to-end  ✅ **SHIPPED**
Migration 023. The final lifecycle stage — **and the loop now closes: ALL Customer Lifecycle stages (BD → Sales → Onboarding → Success → Renewal) are live product ✅.** The pipeline is a **working cache, not a CRM** (SoR principle): `source` + `external_ref` on every opportunity, CSV import labeled bootstrap, native mode for tenants without a CRM. Guarded stage transitions (won/lost only via close RPCs; lost requires a reason; stage_history + audit on close), `close_opportunity_won` = the lifecycle handoff (account created/linked + optional onboarding kickoff via the 022 machinery), `opportunity_won` event key on the R7 trigger machinery (welcome/kickoff plays fire automatically, per-opportunity cooldown), `pipeline_summary` win-rate view. One opportunities table, two lenses: BD (prospect → qualify) and Sales (qualified → won/lost, table + stage select). **Honest:** owner free-text v1; ARR = deal amount on won; no drag-drop kanban. **The CRM connector (Salesforce/HubSpot) is the sync upgrade — trigger: the first tenant that already runs a CRM.**

## Specialist system

### Technical Specialist v1  ✅ **SHIPPED**
Migration 024 + `specialist-consult` edge function. Consulted when completing tasks; highly configurable. Per-source **access modes are the customer's storage choice**, enforced by a DB constraint matrix: ingest (stored/indexed), fetch-only (read at consult time, never persisted), reference (registered + cited). Sources: tag-scoped knowledge, connectors (ingest = synced cache, fetch-only = live read-through), MCP servers (registration + connectivity ping v1 — honest "full MCP session upgrade pending"), links, and a media library (quality flags, .txt/.md extracted instantly, pdf/video honest `extracted=false`). Consultations are recorded with citations; the LLM answer path is dormant-honest behind `ANTHROPIC_API_KEY` (retrieval plumbing verifiable now). `consult_specialist` playbook primitive with confidence floor + escalate/continue. **Scribe sub-specialist:** write-backs are structurally grounded — FK-required consultation, server-side whitelisted payload templates (no model free text), always human-gated, audited with the citation chain.

### Connector Hub + Evidence Pipeline  ✅ **SHIPPED**
Migration 026 + `connector-hub` + `mcp-client` edge functions + `specialist-consult` `resolve_inquiry`. Answers the founder's TCP challenge: connect the customer's real systems and gather evidence BEFORE answering. Six providers behind one adapter interface (Salesforce OAuth2 client-credentials + SOQL, Confluence CQL, Jira JQL, Intercom articles+conversations, Zendesk, generic REST for the customer's own product API; SharePoint registered honestly as not-implemented). Per-connector **role** (product_system/crm/support_desk/knowledge_base) routes the evidence pipeline: account context → knowledge (internal chunks + knowledge-base connectors) → past-case verification → cited evidence bundle with confidence inputs, persisted to `evidence_runs`, every step audited. Per-connector **access_mode** is the customer's storage choice — fetch-only connectors REFUSE sync server-side; read-through persists only citation metadata (snippet ≤200 chars). Real MCP client (Streamable HTTP: initialize/session/tools list/tools call, JSON + SSE) replaces the ping-only v1 — the "full MCP session upgrade" is delivered. **Proven live:** generic_rest end-to-end against a real external API; MCP handshake + tool call against a public MCP server; all negative paths. **Pending founder credentials:** salesforce/confluence/jira/intercom against live instances (free accounts; help text in the wizard shows how).

### Category Contract Layer + Connector Health + Connection Ladder  ✅ **SHIPPED**
Migration 027 + `_shared/categoryContracts.ts` (browser mirror `src/lib/categoryContracts.ts`). **The app now talks in system CATEGORIES; adapters translate.** Nine categories (crm, helpdesk, knowledge_base, erp_financials, billing, payroll_hcm, pos, product_system, other), each with canonical objects + ops in one shared module; `connectors.role` migrated to `connectors.category` (support_desk→helpdesk etc.; old column kept deprecated for rollout safety, then dropped outright in migration 033 once cleanup pass #7 confirmed zero remaining reads). `connector-hub` `category_op`: validates op legality (`op_not_legal_for_category` with the legal list), translates to the provider adapter (`op_not_supported` when it honestly can't), returns canonical-shaped items with the customer's `field_map` ({canonical→source field}, wizard-editable) applied; generic_rest customers bind category ops to their own API paths via `config.endpoints.category_ops` (search_*/get_* fall back to the generic search/record endpoints). Consumers refactored to category language: `resolve_inquiry` v4 (account ← product_system.search_records else crm.search_accounts; knowledge ← knowledge_base.search_articles; history ← helpdesk.search_tickets + crm.search_conversations; every evidence step records category+op+provider), playbook `connector_action` accepts the category-op form (read-through) alongside the legacy zendesk write-back form (compat shim; no live playbook versions existed, verified). **Call-driven connector health:** every hub call updates last_ok_at/last_error_at/consecutive_failures; computed status (healthy/degraded/down/never_connected) badge + "Run health check" on the Connectors page — no cron by design (scheduled checks: first paying tenant). **5-rung connection ladder in the wizard** (category first, provider second): MCP → aggregator ("Available on request" — build trigger: first paying tenant with long-tail needs, deliberately not Merge/Apideck now) → named adapter → your own API via Generic REST → file import. **Honest limits:** salesforce/confluence/jira/intercom category-op translators are shaped to documented APIs but unverified against live instances until credentials exist (same boundary as the hub itself); erp_financials/billing/payroll_hcm/pos have contracts but no named adapters yet — generic_rest bindings serve them today.

### Declarative Adapter Framework  ✅ **SHIPPED**
Migration 028 + `_shared/adapterTemplates.ts` (browser mirror `src/lib/adapterTemplates.ts`) + `connector-hub` v4. **Connecting any REST system is configuration (data), not code** — the founder's "no case-by-case work" ask. `adapter_templates` (platform scope = shared library readable by all; tenant scope = the workspace's own custom systems; writes only via SECURITY DEFINER RPCs `save_adapter_template`/`publish_adapter_template` with membership guard + structural validation mirroring the category contract). A template declares the auth **recipe** (api_key_header/bearer/basic/oauth2_client_credentials/none — secret values stay in `connector_secrets`, never in templates), base-URL shape with per-connector variables, per-op HTTP bindings ({query}/{ref}/{var} templating) and response dot-paths. New provider `template` + `connectors.template_id`; the hub executor resolves template → auth (incl. live OAuth2 token dance) → render → fetch → dot-path walk → canonical items, same health/audit/access-mode enforcement as every provider (fetch-only sync refusal verified). Structured errors for every failure class — `path_not_found_in_response` lists the keys actually found. **Guided builder in Connectors** (5 steps, plain language) with live "Test now" dry-run action (`template_dry_run`: creds in-flight only, never stored; raw response shown side-by-side with extracted items) + **template library** ("Use this template" → prefilled connect). **Seeded platform library (7):** Freshdesk, Gorgias (helpdesk), HubSpot (crm), Chargebee (billing), BambooHR (payroll_hcm), Square (pos), Xero (erp_financials) — every founder-named category now has a concrete path. **Honest limits:** seeded templates are COMMUNITY templates shaped to public API docs, explicitly marked untested-until-connected in the UI; template knowledge ingest not built (read-through only — same as generic_rest); OAuth2 token fetched per call (no cross-call cache). **Verified live:** jsonplaceholder template end-to-end through the template executor (nested dot-paths), header-echo proof that the auth recipe actually sends the key, secret-leak inspection (value only in the service-role-only store), validation negatives, `resolve_inquiry` rerun with evidence steps showing provider=template.

### Legal / Finance / People specialists  `config reuse`
The framework is generic — installing another domain is a charter seed + sources config, not new machinery. Trigger: first tenant needing one.

### Specialist upgrades  `deferred with triggers`
~~Full MCP client sessions~~ **shipped with the Connector Hub (`mcp-client`)**, pdf/docx/video/image content extraction (trigger: R1 activation + first tenant media corpus), Scribe trust-dial expansion beyond always-gated (trigger: earned trust — clean gated write history).

## Sequencing
R2, R4, R5 build in parallel now (R4+R5 fully testable pre-key; R2 testable to credential boundary). R3 follows immediately after (shares de-answer surface). R1 executes the moment the key arrives and re-tests everything the others shipped. Then: **pressure-test rescore** against this scorecard.

## Explicitly deferred (triggers unchanged)
Queue/tiering (volume), ~~tenant playbook builder (after R4)~~ **shipped as R6**, LLM-judge guardrails (after R1 economics), Trust & Architecture page (first security review), PDF/DOCX ingestion (first tenant needing it), signed widget JWTs (first embedded pilot).

## Data Access Grants  ✅ **SHIPPED**
Migration 029 + enforcement in `connector-hub` / `specialist-consult` / `playbook-execute` + Governance → Data Access UI. Founder ask: "we won't want a support DE having access to company financials." Default-deny, per-DE/specialist × system permissions on a cumulative ladder (search ⊂ read ⊂ ingest ⊂ write_back); connector-specific grants beat category grants; all changes via audited role-guarded RPCs; denials audited (`data_access_denied`) and surfaced (evidence-trail `denied_no_access` badge, recent-denials list). Seeded sensible defaults per domain — never financials/billing/payroll. Verified live end-to-end incl. playbook step failure + escalation and Scribe write refusal. *Named upgrade (shipped below):* per-DE knowledge scopes.

## Per-DE Knowledge Scopes  ✅ **SHIPPED**
Migration 030 + enforcement in `match_doc_chunks`/`visible_knowledge_docs` RPCs, wired into `de-answer` / `widget-ask` / `specialist-consult` + Knowledge Library "Who can use this" UI. Closes the named upgrade above. Knowledge defaults to tenant-wide-visible (unlike connectors' default-deny — content the tenant gave the workforce, not a reach into a system of record); optional per-doc scoping to specific DEs/specialists via the audited `set_doc_scope` RPC. Verified live: SSO doc scoped to the Technical Specialist only → specialist retrieval unaffected, a different DE subject got `no_docs`; reverted → both retrieve; anon (non-member) write rejected; audit chain intact (213 checked, 0 broken); Leanne regression reproduced with scoping machinery live; demo tenant untouched.

## Playbooks — "document that executes"  ✅ **SHIPPED**
Migration 031 + `playbook-execute` v8 + full `LivePlaybookBuilder.tsx` overhaul. Research basis: a linear step-list editor, NOT a node canvas (canvases become maintenance burdens for non-technical operators — the founder's own instruction, backed by SOP-tool adoption research); media embedded directly inside steps (SOP-tool adoption lesson: media as an afterthought/attachment kills adoption); instructions versioned and validated exactly like the existing typed-primitive playbook steps (same publish → immutable-snapshot doctrine as R6); branching rendered as simple indented lists, never canvas edges. Five new step types: **instruction** (markdown + embedded image/video, feeds an `instructions_context` accumulator for later ai/consult steps — dormant until LLM activation), **decision** (plain-language operator against an EARLIER step's output, one level of branch nesting, indented then/else rendering), **checklist** (human-tick list, rides the existing human-gate machinery as a new gate type), **wait** (parks the run, resumed by the EXISTING 5-minute dispatch cron — no new infrastructure), **sub_playbook** (runs another published playbook as a real child run, `parent_run_id`-linked, inheriting the parent's DE subject for data-access-grant enforcement — a child can never reach further than its parent). `de_playbook_charter`: the DE operating charter — which playbooks a DE runs and in what priority order; the dispatcher orders due schedules/event fires by charter priority (lowest first) when several playbooks match the same trigger. Builder UI: grouped step picker (Do something / Guide & explain / Flow control), inline plain-language validation errors, a dedicated "document that executes" SOP-style read view (numbered steps, rich instruction blocks with inline media, indented decision branches, action chips), and a server-side dry-run preview mode that simulates every write/connector/gate while running flow-control logic for real — no persistence, no external calls. Verified live end-to-end on a real tenant: all five new step types exercised together in one playbook (including both decision branches on separate runs), checklist human-gate resume, a REAL 65+ second timed wait resumed by the dispatch cron, and a sub_playbook child run whose inherited DE subject was correctly denied a connector call by data-access-grants (default-deny holds across the parent/child boundary). Negative tests all rejected as designed: 2-level decision nesting, forward-referencing decisions, unpublished/cyclical sub_playbook references. Priority-ordered dispatch proven by flipping two charter priorities and observing the evaluation order flip. Legacy `renewal_v1` regression-verified unaffected (auto-approved and gated-then-approved paths both still work). `verify_audit_chain` intact on both tenants touched. *Honest limits:* `instructions_context` is dormant until a consult step's LLM is activated; wait resume lands within the existing 0–5 minute dispatch-tick window, not to-the-second; decision nesting is a deliberate v1 simplicity cap, not a technical ceiling — deeper branching should be modeled as a sub_playbook.

## Knowledge Feedback Loop  ✅ **SHIPPED** — dogfooded on Acme Telecom test tenant
Migration 032. Closes the founder's loop: "how the DE will act to resolve a support inquiry and send back feedback to knowledgebase whether it was accurate or needs improvement with human in loop for verification." `evidence_feedback` (human verdict on an evidence run — accurate/needs_improvement/inaccurate + notes) + `knowledge_revision_requests` (a proposed knowledge change, template-composed server-side from the current doc + evidence gaps + reviewer note — never free-form LLM text, same anti-hallucination doctrine as the Scribe) + `knowledge_docs.previous_version_id`/`is_current` (an honest, non-destructive version chain). `submit_evidence_feedback` auto-creates a pending revision request + a `knowledge_revision` human task on any non-accurate verdict — this is **decideHumanTask hook #5**, additive, hooks 1–4 untouched. `apply_knowledge_revision` inserts a new doc version and the client re-runs the SAME `ingest-chunks` path every doc uses; `reject_knowledge_revision` changes nothing. UI: a verdict control on every Technical Specialist evidence run, and a "Knowledge Revisions" panel on Knowledge Library with a diff view and Approve/Reject. **Dogfooded live on Acme Telecom** (real, non-demo tenant): connected a real "Test CRM Connection" via generic_rest against jsonplaceholder.typicode.com; granted the Technical Specialist explicit access; ran `resolve_inquiry` for a real Leanne Graham login inquiry — full evidence trail with live account context, 1 knowledge hit, honest zero history corroboration; submitted `needs_improvement` feedback with a 2FA note → auto-created revision request + task; approved as a second reviewer → new doc version created, re-embedded, and a re-run of the SAME inquiry retrieved the improved content (citation pointed at the new doc id); negative tests confirmed 'accurate' creates no revision and a rejected revision leaves the doc completely unchanged; `verify_audit_chain` intact (239 checked, 0 broken); demo tenant untouched. *Honest limits:* proposed content is template-assembled, not LLM-drafted; one doc proposed per revision (no multi-doc merge); no dedicated full-history viewer yet (chain is queryable, proven live, not yet a UI list); answer step stays dormant until LLM activation, same as every evidence run.

## Gap-Analysis Tier 0, items 3 + 4 — Proactive Inquiry Triage + Live "DE at Work" Queue  ✅ **SHIPPED**
Migration 034. Closes the two platform-blocking gaps identified in the pre-build-phase audit (see memory/gap_analysis_roadmap.md): nothing noticed new work and acted unprompted, and there was no near-real-time view of a DE's reasoning while it worked. **Research basis:** Decagon/Sierra escalation doctrine (confidence threshold + guardrails-always-win composition — reused, not reinvented, from the existing `de_autonomy`/`trust_policies` (R5.5) and `guardrail_rules` (015) machinery); 2026 agent-observability doctrine (semantic telemetry — the reasoning — over structural status dots). `inbox_watch_state` (idempotent per-connector polling cursor) + `evidence_run_decisions` (companion to `evidence_runs`: source manual/proactive_trigger/manual_simulation, decision would_auto_send/needs_review/blocked_guardrail/skipped_no_access, deterministic confidence, plain-language reasoning, linked human task). `decide_inquiry_triage` (SQL) mirrors `generateInvoice`'s composition rule exactly, transplanted onto the `answer_widget` trust-dial category that R5.5 stored but left dormant ("consumed only on activation" — this ships that activation for the proactive path specifically, not yet de-answer/widget-ask, which remain R1's job). `poll_support_inbox` (specialist-consult action) resolves qualifying (helpdesk connector × DE-or-specialist subject) pairs via data-access-grants (029), diffs against the watch cursor, and runs each new item through `runResolveInquiry` — the SAME evidence pipeline `resolve_inquiry` already used, extracted into one shared function so both paths are identical. **No new pg_cron job** — piggybacks the existing 5-minute `invoke_playbook_dispatch()` tick with a second, independent `net.http_post`. `simulate_inquiry` gives a demo-safe manual trigger, always tagged `manual_simulation`, never conflated with the real automatic path. New "DE at Work" page under Operations: decision badges + plain-language reasoning per run, expandable evidence steps, 8-second poll (this codebase has no existing Realtime channel anywhere, so polling — the established pattern — was chosen over introducing one for the first time here). **Verified live on Acme Telecom:** all four decision outcomes proven with real evidence runs (`would_auto_send` at 48% confidence against a 40% floor; `needs_review` creating a real `inquiry_review` human task; `blocked_guardrail` citing a seeded "no unilateral refund promises" rule even at auto-send-eligible confidence; `skipped_no_access` reproduced via a genuine mid-poll access-revocation race, honestly distinct from the zero-target no-op when a grant never existed); idempotency confirmed (re-running the poll immediately reprocessed zero items); `resolve_inquiry`/`dispatch_due_triggers`/the cron job all regression-verified unaffected; audit chain 0 broken links; demo tenant 0 writes; `tsc`/`vite build` clean; UI verified live in-browser including the simulation control and badges. *Honest limits:* `would_auto_send` records intent only — no outbound reply channel exists yet (tracked separately as R1/roadmap item 2), so nothing is ever actually sent to a customer; no sentiment/frustration/repeat-contact detection (explicitly out of scope this pass); polling is 5-minute-tick-driven, not event-pushed.

## The Generalized Action Layer  ✅ **SHIPPED**
Migration 035. The founder's core architectural correction, built: "we are not replicating any other support bot but a human who will not just understand the request and get information from the available systems... but also will act where required... build scalable DEs, playbooks, guardrails, specialists" (see `memory/feedback_de_genericity_test.md` — this closes the gap that memo documented: retrieval was fully generalized via category contracts (027), acting was not — the only write paths were a whitelisted Scribe `action_key` enum, Zendesk-only). **Research basis:** MCP tool annotations (`readOnlyHint`/`destructiveHint`/`idempotentHint`/`openWorldHint`) supply the risk vocabulary — `action_definitions.risk` is exactly `{destructive, idempotent}`; typed action contracts + the shadow→suggest→autonomy progressive-trust model map onto the EXISTING earned-trust ladder (`de_autonomy`/`trust_policies`, R5.5) via a new `action_execute` category — no parallel trust system. **New composition rule, founder-mandated:** a destructive action ALWAYS requires human approval, no trust tier overrides it — a hard platform floor in `decide_action_execution`, sitting even above guardrail-always-wins (not a `guardrail_rules` row a tenant could edit).

`action_definitions` (scope platform/tenant, category = one of the 9 category-contract categories, `provider` = `template` (extends `adapter_templates` with an `actions` map — the SAME variable-substitution engine `ops` uses, widened to POST/PUT/PATCH/DELETE) or a named provider like `zendesk`, `param_schema`, `risk`, `execution`). `scribe_requests` gained a nullable `action_definition_id` FK — additive; the `action_key` CHECK widened (not replaced) to add `reply_to_ticket` alongside the original two, every existing row/path untouched. `decide_action_execution` (SQL) composes destructive-always-gates (checked first, unconditionally) → guardrail-always-wins → trust-narrows-within-it, mirroring `decide_inquiry_triage`/`generateInvoice` 1:1. `connector-hub` gained `preview_action` (renders the exact request via a pure `renderAction` function shared with execute, never calls out, plain-language receipt preview) and `execute_action` (write_back access check → the composition → actual call + plain-language RECEIPT, never raw JSON). The Scribe's `decideHumanTask` hook #3 absorbed the new `action_approval` task type rather than growing a new hook number. The playbook `connector_action` step gained a third form (`action_category`/`action_key`/`param_templates`) so a playbook can act on any registered action in any category through the identical generalized path, access-grants enforced exactly like every other connector step.

Seeded: `add_internal_note`/`update_status` (Zendesk, unchanged from the original Scribe) + `reply_to_ticket` (Zendesk, NEW, destructive — proves helpdesk has 3 actions, not 2) + `create_test_record` (template provider, non-destructive, verification-only against jsonplaceholder). **Verified live on Acme Telecom:** `preview_action` rendered the exact POST with zero calls; `execute_action` at low trust created a `human_gated_trust` task with no external call, then raising trust via the REAL `trust_apply_level` machinery flipped the identical action to `auto_executed` with a genuine `HTTP 201` receipt; **destructive-always-gates held at MAX trust** (`update_status`, destructive=true, still `human_gated_destructive` — side-by-side against the same tenant/trust-level with `destructive=false` returning `auto_executed`, isolating the destructive flag as the only variable); **guardrail-always-wins held at max trust** (a temporary blocking rule forced `guardrail_blocked` despite `action_execute` being enabled; removing it restored auto-execution); data-access-grant revocation produced honest `access_denied`, not a silent skip; a live playbook run using the new `connector_action` action-key form failed honestly on a missing grant, then completed with a real receipt after granting it. Regression: Scribe's original two action branches byte-identical; audit chain 0 broken links; demo tenant 0 writes; `tsc`/`vite build` clean. *Honest limits:* only 4 actions seeded platform-wide (mechanism is generic, catalog is narrow by design this pass); `action_execute` trust is tenant-wide binary, not yet tiered like `invoice_auto_send`'s amount ladder; no live Zendesk credentials existed this session, so the two original + the new reply action were verified by code-path inspection plus the template-provider action's live proof, not a live Zendesk round-trip; the trigger/polling layer (034) is explicitly out of scope — a follow-up generalizes proactive triggering the way this build generalized execution.

## The Generalized Trigger Layer  ✅ **SHIPPED — closes the genericity gap**
Migration 036. Part 2 of 2 of the founder's core architectural correction (see `memory/feedback_de_genericity_test.md`). Retrieval was generalized (category contracts, 027). Acting was generalized (the action layer, 035). The trigger was not: `poll_support_inbox` (034) only ever polled `category='helpdesk'` connectors and framed every new item as support-inquiry language — the next domain would have tempted `poll_account_signals`/`poll_finance_documents` as separate bespoke pollers, exactly the per-department-bot pattern rejected from day one. This migration removes that temptation at the root.

`de_autonomy`/`trust_policies` gain a nullable `source_category` column — trust for `action_execute` (and any future category-scoped decision) now resolves PER SOURCE CATEGORY without ever widening the action-type CHECK enum again, closing the "one more named category forever" treadmill that `trust_level_settings`'s hardcoded per-category branching was on track to repeat. `work_item_framing` — platform-default + tenant-overridable, per-category plain-language templates ("New CRM record needs review: {title} — {snippet}") turning a raw canonical item into the description fed into the evidence pipeline — configuration, not hardcoded strings. `poll_de_work_sources_targets` — `poll_support_inbox_targets` minus the `where c.category = 'helpdesk'` filter, plus the connector's own category as an output column. `decide_work_item_triage` — `decide_inquiry_triage`'s category-parameterized sibling, same guardrail-then-trust composition, with a legacy fallback so existing tenant configuration keeps working unchanged. `evidence_run_decisions.decision` gains `would_act`/`acted` — the act-side siblings of `would_auto_send`/`needs_review` — plus `source_category` and a link to the real `action_executions` receipt.

**`poll_de_work_sources`** (replacing `poll_support_inbox` as the cron target, `poll_support_inbox` retired outright — not left dangling, the old action string redirects to the same new handler): iterates ALL 9 categories per tenant, `list_recent` + idempotent cursor diff (unchanged), frames each new item via the category's template, runs the SAME `runResolveInquiry` pipeline (zero diff lines — confirmed via `git diff`), decides via `decide_work_item_triage`, and — new — if the decision clears the bar AND a registered `action_definition` exists for the category, calls connector-hub's `execute_action` for real: `would_act` (gated, with a real approval task) or `acted` (auto-executed, real receipt), falling back honestly to `would_auto_send` when no action is registered or its params can't be filled from the item alone. Same 5-minute cron piggyback, zero new pg_cron jobs.

**THE ACCEPTANCE TEST, proven live on a SECOND category with ZERO new code:** reused Acme Telecom's existing `crm`-category connector (jsonplaceholder `/users`, already connected — no new connector needed), registered ONE new `action_definition` (`category='crm'`, reusing the existing jsonplaceholder template), and granted the EXISTING Technical Specialist write-back — no new "Account DE" persona, deliberately (that's a separate, smaller-than-before follow-up, see below). Running `poll_de_work_sources` discovered 5 new CRM items via the identical code path (no `if category==='crm'` branch anywhere), framed them with CRM-appropriate language (not "customer says X"), and — with trust already enabled from the 035 verification — **actually executed real HTTP round-trips**: `decisions: {"acted": 5}`, genuine `HTTP 201` receipts against jsonplaceholder. Notice → understand → decide → ACT, for a domain that is not support, zero new bespoke code. Also reproduced in the generalized path: idempotency (re-poll found 0 new items), destructive-always-gates (flipping the action to destructive turned all 5 into `would_act`/`human_gated_destructive` at the SAME trust level, receipt null, real approval task created), guardrail-always-wins (a temporary blocking rule forced `guardrail_blocked`, removing it restored `acted` on the next tick), and `skipped_no_access` for a genuine mid-flight grant revocation (distinct from the honest zero-target no-op when no grant ever existed). Regression: the original helpdesk path behaves identically (cursors already advanced from 034's own verification); `dispatch_due_triggers`/the cron job unaffected (still one job, same schedule); manual `resolve_inquiry`/`simulate_inquiry` unaffected by construction (the shared pipeline function has zero diff lines). Audit chain: 1771 events checked on Acme Telecom, 0 broken links. Demo tenant: 0 writes. `tsc`/`vite build` clean.

*Honest limits:* this proves the TRIGGER MECHANISM generalizes — it does NOT stand up a real Account or Finance DE persona (gap-analysis items 14/19 below are revised in light of this). A real bug was found and fixed during this build's own verification: `resolve_action_definition_for_category`'s first draft returned a bare composite type, which Postgres turns into one row of all-NULLs (not zero rows) when nothing matches — fixed by declaring it `SETOF`, documented rather than hidden. `action_execute` trust is still tenant-wide binary in practice (no tenant has yet configured a value that actually diverges per category — the new column makes it possible, the CRM proof ran at the same tenant-wide level `product_system` already used). Framing templates are simple `{title}`/`{snippet}` substitution, not a full template engine.

**Revised scope for gap-analysis items 14/19 (Account DE, Finance DE):** the leverage insight from the prior build ("de_autonomy, earned-trust, playbook charters, and access grants are all generic and already built... giving Account/Finance a real DE is mostly a persona row + assigned playbook + one outbound action, not new engineering") is now proven, not just argued — this build demonstrated the FULL notice→understand→decide→act loop firing on a non-support category using only a persona-adjacent grant + one action_definition row, zero new code. Standing up Account or Finance for real is now genuinely a configuration exercise: a persona row (digital_employees or specialist_profiles), a charter/playbook assignment, `seed_default_grants`-style category grants scoped to what that domain needs, and 1-2 real `action_definitions` for whatever it needs to write — no new trigger code, no new evidence-pipeline code, no new trust/guardrail composition code. The estimate for items 14/19 should shrink accordingly on the next roadmap pass.

## The Real Account Digital Employee  ✅ **SHIPPED**
Migration 037. Stood up "Jordan," the Account Success DE, proving the 036 prediction above: everything after ONE explicitly-sanctioned new capability (`create_digital_employee` — no generic way to create a DE persona existed at all until this build) was pure configuration on the now-generic machinery — `data_access_grants` (crm write_back only, explicitly no financials/billing/payroll), `de_playbook_charter` (`account_at_risk_checkin`, built from existing step primitives only), `action_definitions` (one new `log_checkin_note` row reusing the shared 036 adapter template), `trust_policies`/`de_autonomy` (a fresh crm-scoped `action_execute` policy starting at level 0). Fixed one real generalization gap surfaced by this build's own acceptance test: `decide_action_execution` (035, the write/act composition) had never been taught to resolve `source_category` the way `decide_work_item_triage` (036) already had — a fresh category-scoped trust policy was silently falling back to an unrelated tenant-wide row until fixed at the primitive level. Also restored two pre-existing regressions found the same way (a dropped nightly health-recompute cron step, and an entirely dropped `account_at_risk` dispatcher branch — both silently lost when later migrations did full-function `REPLACE`s rather than additive changes). Verified live end-to-end on Acme Telecom: manufactured real at-risk signals, the account genuinely flipped `active → at_risk`, the trigger fired through `dispatch_due_triggers` → a real playbook run → a real gated action + human follow-up task. *Honest limits:* only one action registered (`log_checkin_note`); no live financial/billing connector exists yet to prove the "excluded from financials" claim beyond the grant table itself.

## Gap-Analysis Tier 2, item 27 — Generalized "Nothing Happened, Escalate" Watchdog  ✅ **SHIPPED — closes items 6 + 12**
Migration 042. The gap-analysis roadmap's own leverage insight: "items 6 (Support SLA), 12 (Onboarding stalled projects), and 16 (Account at-risk auto-execution) are the same missing primitive wearing three names... build ONE watchdog on the existing 5-min dispatch cron; wire it into all three." Item 16 was already closed by migration 037's `account_at_risk` event rule (a health-STATE-CHANGE trigger — a genuinely different primitive from this one). This build closes items 6 and 12 as ONE domain-agnostic primitive, per the standing genericity test (`memory/feedback_de_genericity_test.md`).

**Research basis:** mature SLA/escalation systems (PagerDuty, Zendesk SLA policies, ServiceNow OLAs) use TIERED severity — a warning before the hard breach, not one alarm — and rely on a cooldown so the same stale item doesn't re-fire on every evaluation tick. Both are implemented directly: `staleness_policies` (tenant_id, `target_kind` — a free-text configuration key, NOT a widened enum, same "no enum widening" lesson as 036's `source_category` — `warning_after`/`breach_after` intervals, seeded per-tenant with sensible defaults: onboarding_project 14d/30d, pending_review_task 24h/72h, fully tenant-overridable) and `staleness_escalations` (one row per tenant/target_kind/target_id/tier; a partial unique index on unresolved rows IS the cooldown — one open escalation per exact tier per target at a time; a tier change is a new row; a resolved-then-restale item can escalate again fresh).

`check_staleness(tenant_id)` — SECURITY DEFINER, explicit `tenant_id` filter on every query (no RLS reliance — runs via the cron dispatcher, exactly like `dispatch_due_triggers`/`poll_de_work_sources_targets`), granted service_role-only. Has NO branch-tree of per-domain business logic: the outer loop (which policies, which tenant, which tier) is 100% generic; there are exactly two small, structurally-identical inner lookups (onboarding_projects.updated_at while active; human_tasks.created_at while pending, covering `inquiry_review`/`action_approval`/`checklist`/`review_gate`/`approval_gate` — Support's and the Account DE's pending tasks alike, through the SAME code path with no per-type branching). A third target_kind reusing either shape needs only a new policy row, zero code. Tier/cooldown/task-creation logic is written once, in a shared `stale_upsert_escalation` helper. No new pg_cron job: `check_staleness` is pure SQL with no HTTP need, so `invoke_playbook_dispatch()` calls it directly inline, wrapped in its own exception handler.

**This build reproduced the exact migration-040 tenant-isolation gotcha live, firsthand, before catching and fixing it:** `revoke all ... from public` did not strip an already-existing per-role grant to anon/authenticated on `stale_upsert_escalation` (PUBLIC and named-role grants are independent ACL entries) — caught via `information_schema.routine_privileges`, fixed with an explicit `from public, anon, authenticated` revoke on all three new functions, reverified clean after a full migration re-application.

**Verified live on Acme Telecom, full cycle:** a real onboarding project backdated past warning then breach thresholds produced two SEPARATE escalations (not a duplicate), each with a correctly humanized plain-language message; a re-run with nothing changed created no duplicates (cooldown proven); marking the project completed resolved both. The SAME cycle proven on a real pre-existing `action_approval` task from the Account DE build — swept into a breach-tier escalation by the identical code path, deduped on re-run, resolved on approval — the genericity acceptance test this build required. All test-induced rows cleaned up afterward; tenant policies restored to realistic defaults; demo tenant 0 writes; `tsc`/`vite build` clean.

*Honest limits:* no outbound notification (email/Slack) — surfaces as an in-app human_task like every other DE-raised task, gated on the same open outbound-channel gap as roadmap item 2. Only two target_kind SHAPES wired today; a new domain reusing either is policy-only, but a genuinely new table shape needs one small new lookup branch (stated honestly, not oversold as infinitely automatic). UI: a "Stalled work" stat + tier-colored badge + filter toggle on the existing Human Tasks page, not a new page.

## The Real Finance Digital Employee  ✅ **SHIPPED — second proof of the configuration-only genericity claim**
Migration 043. Closes gap-analysis items 19-22 (Finance was the weakest-scored function, ~10% built at last audit). Stood up "Sasha Reyes," the Finance DE, for Acme Telecom, using the exact persona+charter+grants+actions pattern migration 037 proved for Jordan (Account DE) — the acceptance test for whether that pattern generalizes to a SECOND, independently-designed department, not just a repeated claim.

**Configuration only, reusing 037's own machinery unmodified:** persona row (no new "add a DE" capability needed — 037 already built `create_digital_employee`; shows in the existing roster UI with zero UI changes). `data_access_grants` — exactly ONE grant, `erp_financials: write_back`, explicitly no `crm`, no `payroll_hcm`. `action_definitions` (035) — three new rows: `send_payment_reminder` (non-destructive), `send_final_notice` + `flag_for_collections` (both destructive=true), reusing the SAME shared verification adapter template 036/037 already used. `trust_policies`/`de_autonomy` — a fresh `erp_financials`-scoped `action_execute` policy at level 0. `guardrail_rules` — one legal-threat `blocked_phrase` rule, one `require_approval_over_cents` rule at $25,000. Two real dunning playbooks (`dunning_payment_reminder` at 3+ days overdue, `dunning_final_notice` at 14+ days overdue) from existing step primitives only. `playbook_event_rules` — two rows on the ALREADY-EXISTING `invoice_overdue` event key (different `overdue_days` params, zero enum widening). `staleness_policies` — one new `target_kind='overdue_invoice_unattended'` row.

**Research grounding (2026 dunning/collections practice, cited in the migration):** early-stage reminders are low-risk and safe to automate once trust is earned; a firmer "final notice" needs a higher bar; anything resembling a credit hold, collections referral, or legal threat must ALWAYS require a human, matching the platform's destructive-always-gates floor exactly; high-value accounts always route to a human regardless of trust tier.

**Three genuine pre-existing gaps found and fixed at the primitive level, all surfaced by this build's own live verification, not hypothesized:** (1) `trust_apply_level` (025) was never updated when migration 036 widened `de_autonomy`'s unique index to include `source_category` — its `ON CONFLICT` target no longer matched any real constraint, so promoting ANY category-scoped trust policy failed outright, and `apply_trust_promotion` never even passed `source_category` through — meaning this has silently affected the Account DE's own crm-scoped policy since 037 shipped. Fixed by threading a nullable `p_source_category` param through both functions (backward compatible) and correcting the conflict target; the stale 4-arg signature was dropped, not left dangling. (2) An early version of this build's dunning playbooks put the send action at the top level after a `decision` gate — but `decision` branches run as additional inline steps and the engine unconditionally continues to the next top-level step regardless of branch taken, silently defeating the high-value guardrail. (3) Moving the send into `else_steps` surfaced that `BRANCH_ALLOWED` had always listed `connector_action` as branch-legal at validation time, but the branch executor never actually implemented it (silent no-op for any department that ever tried this). Fixed by extracting the action-execution logic into a shared `execRegisteredAction` helper used by both the top-level switch and the branch executor (`playbook-execute` v11), plus making `check_account` populate `step.output` so a `decision` step can actually read `arr_cents` (previously always resolved `undefined`, making any `greater_than` check always false).

**Verified live:** trust-ladder proof (level-0 → gated `human_gated_trust` for the reminder → promoted via the real, now-fixed `trust_apply_level` → `auto_executed`, confirmed with a genuine `HTTP 201` receipt against the connected AR Ledger connector). **Destructive-always-gates proof (the most important one)** — with `erp_financials` trust at its fully-enabled state, `send_final_notice` and `flag_for_collections` both still return `human_gated_destructive` unconditionally, the platform's core safety promise applied to money. High-value-account guardrail proof (post-fix) — a $30,000-ARR test account correctly took the "then" branch and the send action never ran (`status: "pending"`, untouched); reset to $5,000 ARR, the "else" branch correctly sent for real. Discovery-pipeline proof — the real dispatch-secret `poll_de_work_sources` path discovered items on the new `erp_financials` category via the identical generic code path migration 036 proved, zero new business-logic branches. Data-access exclusion — exactly one grant, CRM access denied (`reason: no_grant`). Security — `information_schema.routine_privileges` AND `has_function_privilege('public', ...)` both checked on every new/modified function; found and fixed `invoke_playbook_dispatch` still carrying live anon/authenticated grants left over from 037/042 (neither had revoked those explicitly) — a genuine, independently-discovered instance of the migration-040 gotcha. Regression: Support and Jordan's existing trust dials unaffected (isolation confirmed); audit chain 0 null hashes; demo tenant untouched; `tsc`/`vite build` clean.

*Honest limits:* the trust dial is one boolean per (tenant, category) with no per-action-key granularity — the brief's three-tier ask collapses into the platform's real two-tier vocabulary (auto-capable vs. always-human), so final notice gets the strictest available bar, not a fabricated intermediate one. `decide_action_execution` still doesn't evaluate dollar thresholds at all — the high-value gate is playbook-authored `decision`-step configuration, not a universal composition-function feature. Only one connector (a jsonplaceholder-backed test target) and three actions exist; no real accounting/ERP system is connected. The playbook engine's run context is account-id-scoped only, so this build gates on account ARR rather than the individual invoice amount.

## Gap-Analysis Tier 2, item 26 — Identity & Credential Inventory View  ✅ **SHIPPED**
Migration 044. Closes gap-analysis item 26, the direct follow-up to the adversarial multi-tenant isolation audit (migrations 038-041, `memory/project_isolation_audit.md`): "no single view shows which DE or specialist holds which live credential and grant across every connected system — the first document a serious enterprise security reviewer will ask for." This is the visibility layer on top of what that audit secured — READ-ONLY reporting, no writes.

**What it does:** `get_identity_inventory(p_tenant_id)` assembles, per subject (Digital Employee or Specialist), every connected system it holds any grant on: permission level (resolved connector-specific-beats-category, exactly like `resolve_access`), whether a credential is stored (boolean existence check ONLY — the secret value is never read, matching `connector_secrets`' zero-client-access model since migration 017), connector health (`last_ok_at`/`last_error_at`/`consecutive_failures`, migration 027), earned trust level for that category (`trust_policies.current_level`, migrations 025/036), and which registered `action_definitions` (035) it could invoke there. New Governance page "Identity & Credentials" — one card per identity, expandable to its systems, plain-language framing throughout ("Jordan can write to your CRM system... Trust level 0 — always requires human approval") instead of raw enum values.

**Security discipline (applied from the start, not retrofitted — this exact class of mistake recurred 4x earlier today across migrations 040-043):** the RPC does not trust its `p_tenant_id` parameter — it derives the caller's real tenant from `auth.uid()` → `profiles` and raises unless they match (service_role passes through). EXECUTE revoked from `public`, `anon`, AND `authenticated` explicitly before a single re-grant to `authenticated, service_role` — verified directly against `pg_proc.proacl` post-migration, no bare PUBLIC entry.

**Verified live, cross-checked against raw `data_access_grants` rows:** ran for Acme Telecom under a simulated real authenticated session — 11 subject×system rows, matching the raw grant table exactly (Jordan/Account: `crm write_back` on both real crm connectors, trust 0; Sasha Reyes/Finance: `erp_financials write_back`, trust 0; Support DE: `helpdesk read` + `product_system write_back`, trust 1; Technical Specialist: `crm read` category default correctly overridden to `search` by a real connector-specific grant). A genuine bug was found and fixed by this build's own live-verification step: the first version used `connector_id` as both a `RETURNS TABLE` OUT-parameter and an inner CTE alias, which Postgres correctly rejected as ambiguous the moment it was actually run (not caught by `tsc`/build, only by executing it for real) — fixed by renaming the CTE join key.

**No-secret-leakage + tenant-isolation adversarial tests (mirroring today's audit style):** every column key in the live response inspected — zero secret/token/password-shaped keys, `has_stored_credential` boolean only; `connector_secrets` itself confirmed still unreadable under the same session. Forged cross-tenant call (`get_identity_inventory` with the Demo Workspace's tenant id, called as Acme's real user) denied with `not a member of this tenant`; called as bare `anon` denied at the grant level entirely (`permission denied`), never reaching the function body.

**Regression:** `resolve_access`/`set_access_grant`/`revoke_access_grant` ACLs unchanged; Data Access and Trust & Architecture pages unaffected. Audit chain intact (3,802 events, Acme Telecom). Demo tenant confirmed untouched (0 DEs, 0 grants). `tsc --noEmit` and `vite build` both clean.

*Honest limits:* reporting only — access changes still happen on Data Access. Specialists have no per-subject `action_execute` trust row today (DE-scoped only), reported as "no trust policy set" rather than fabricated. "Possible actions" lists the registered write-side catalogue for a category cross-referenced with the grant — not a live execution forecast.
